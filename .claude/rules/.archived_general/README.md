# アーカイブディレクトリ

このディレクトリは使用頻度の低いルールファイルを保存するためのものです。

## 目的

- Claude Code の起動時間を短縮
- コンテキストの整理
- 不要なルールの適用を回避

## 使い方

1. 使用しないルールファイルをこのディレクトリに移動
2. `.claudeignore` で自動的に除外される
3. 必要になったら元の場所に戻す

## 注意事項

- このディレクトリのファイルは Claude Code に読み込まれません
- 削除せずに保持されるため、後で参照可能
