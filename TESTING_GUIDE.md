# Readdy AI Clone - 機能テストガイド

このガイドでは、Readdy AI Cloneの全機能をテストする手順を説明します。

## 前提条件

- ✅ Cloudflare Workers APIがデプロイ済み
- ✅ Workers APIの環境変数（OPENAI_API_KEY等）が設定済み
- ✅ フロントエンドがビルド済み
- ✅ OpenAI APIキーに残高がある

## テスト環境

| 項目 | 値 |
|------|-----|
| Workers API URL | `https://readdy-ai-workers.sayasaya.workers.dev` |
| Frontend URL（開発） | `http://localhost:3000` |
| Frontend URL（本番） | デプロイ後のURL |

## テスト1: Workers APIエンドポイント

### 1.1 ヘルスチェック

```bash
curl https://readdy-ai-workers.sayasaya.workers.dev/health
```

**期待される結果**:
```json
{
  "status": "ok",
  "timestamp": "2025-01-21T..."
}
```

### 1.2 テキストからコード生成

```bash
curl -X POST https://readdy-ai-workers.sayasaya.workers.dev/api/generate \
  -H "Content-Type: application/json" \
  -d '{
    "prompt": "Create a simple React button component with Tailwind CSS",
    "model": "gpt-4o",
    "temperature": 0.7,
    "max_tokens": 1000
  }'
```

**期待される結果**:
```json
{
  "success": true,
  "code": "import React from 'react';\n\nexport default function Button() {\n  return (\n    <button className=\"px-4 py-2 bg-blue-500 text-white rounded hover:bg-blue-600\">\n      Click me\n    </button>\n  );\n}",
  "usage": {
    "prompt_tokens": 25,
    "completion_tokens": 50,
    "total_tokens": 75
  }
}
```

**エラー例**:
```json
{
  "error": "OpenAI API key not configured"
}
```
→ Workers APIの環境変数を確認してください（`ENV_SETUP_GUIDE.md` 参照）

### 1.3 画像からUI生成

```bash
cd workers/api
bash test-generate-from-image.sh
```

**期待される結果**: GPT-4o Visionが画像を解析してReactコードを返す

### 1.4 URLクローン

```bash
cd workers/api
bash test-clone-from-url.sh
```

**期待される結果**: 指定したURLのHTMLを取得してReact/Next.jsコードに変換

## テスト2: フロントエンド機能

### セットアップ

```bash
cd D:\NEXTCLOUD\Web_app\Web_readdy_ai
npm run dev
```

ブラウザで http://localhost:3000 を開く

### 2.1 APIキー設定

1. 右上の **設定** アイコンをクリック
2. 以下を入力:
   - **OpenAI API Key**: `sk-proj-...` または `sk-...`
   - **Supabase URL**: `https://xxxxx.supabase.co`
   - **Supabase Service Role Key**: `eyJ...`
   - **Cloudflare Workers API URL**: `https://readdy-ai-workers.sayasaya.workers.dev`
3. **保存** をクリック

**確認**:
- ブラウザの開発者ツール > Application > Local Storage で値が保存されていることを確認
- 設定画面を再度開いて値が保持されていることを確認

### 2.2 プロジェクト作成 - テキストモード

1. **新規プロジェクト** ボタンをクリック
2. プロジェクト名を入力: `Test Landing Page`
3. フレームワーク: `Next.js`
4. **次へ** をクリック
5. **テキスト** モードを選択（✏️アイコン）
6. プロンプトを入力:
   ```
   Create a modern landing page with:
   - Hero section with gradient background
   - Feature cards (3 columns)
   - Call-to-action button
   - Footer with social links
   Use Tailwind CSS
   ```
7. **作成開始** をクリック
8. 生成中の表示を確認（スピナーアニメーション）

**期待される結果**:
- 数秒後、エディター画面に遷移
- 左パネルにページリスト表示
- 中央にプレビュー表示
- 右パネルにコードエディター表示
- 生成されたコードが表示されている

**確認項目**:
- [ ] コード生成が成功
- [ ] プレビューが正しく表示
- [ ] エラーが表示されていない

### 2.3 プロジェクト作成 - 画像モード

1. **新規プロジェクト** ボタンをクリック
2. プロジェクト名: `Image UI Test`
3. **次へ** をクリック
4. **画像** モードを選択（🖼️アイコン）
5. UIデザイン画像をアップロード（PNGまたはJPEG）
6. 追加の指示（任意）: `Make it responsive and mobile-friendly`
7. **作成開始** をクリック

**期待される結果**:
- 画像がプレビュー表示される
- GPT-4o Visionが画像を解析
- 画像に基づいたReactコードが生成される

**確認項目**:
- [ ] 画像アップロードが機能
- [ ] プレビュー表示が正しい
- [ ] 生成されたコードが画像デザインに近い

### 2.4 プロジェクト作成 - URLモード

1. **新規プロジェクト** ボタンをクリック
2. プロジェクト名: `Clone Test`
3. **次へ** をクリック
4. **URL** モードを選択（🔗アイコン）
5. URL入力: `https://example.com`
6. 追加の指示（任意）: `Convert to dark mode design`
7. **作成開始** をクリック

**期待される結果**:
- 指定URLのHTMLを取得
- React/Next.jsコードに変換
- エディターに表示

**確認項目**:
- [ ] URL検証が機能
- [ ] HTMLフェッチが成功
- [ ] コード変換が成功

### 2.5 エディター機能

プロジェクトを開いた状態で:

#### コード編集
1. 右パネルでコードを直接編集
2. 保存ボタンをクリック
3. プレビューが更新されることを確認

#### プレビューモード切り替え
1. プレビューパネル上部のボタンで切り替え:
   - 🖥️ デスクトップ表示
   - 📱 モバイル表示
2. レスポンシブデザインが正しく表示されることを確認

#### ページ追加
1. 左パネルの **新しいページ** ボタンをクリック
2. パス名を入力: `/about`
3. **作成** をクリック
4. 新しいページが追加される

### 2.6 デプロイ機能 - Vercel

1. プロジェクトを開く
2. **デプロイ** ボタンをクリック
3. **Vercel** を選択
4. Vercel Tokenを入力（設定済みの場合は不要）
5. **Vercelにデプロイ** をクリック

**期待される結果**:
```
✅ デプロイ成功！
公開URL: https://your-app.vercel.app
```

**確認項目**:
- [ ] デプロイが成功
- [ ] 公開URLにアクセス可能
- [ ] アプリが正常に動作

### 2.7 デプロイ機能 - GitHub Pages

1. **デプロイ** ボタンをクリック
2. **GitHub Pages** を選択
3. GitHub Personal Access Tokenを入力
4. GitHubユーザー名を入力
5. **GitHub Pagesにデプロイ** をクリック

**期待される結果**:
```
✅ デプロイ成功！
公開URL: https://username.github.io/project-name
リポジトリ: https://github.com/username/project-name
```

**確認項目**:
- [ ] GitHubリポジトリが作成された
- [ ] gh-pagesブランチが作成された
- [ ] GitHub Pagesが有効化された
- [ ] 公開URLにアクセス可能

### 2.8 デプロイ機能 - Cloudflare Pages

1. **デプロイ** ボタンをクリック
2. **Cloudflare** を選択
3. Cloudflare Account IDを入力
4. Cloudflare API Tokenを入力
5. **Cloudflare Pagesにデプロイ** をクリック

**期待される結果**:
```
✅ デプロイ成功！
公開URL: https://project-name.pages.dev
```

## テスト3: エラーハンドリング

### 3.1 無効なAPIキー

1. 設定で無効なOpenAI APIキーを入力
2. プロジェクト作成を試みる

**期待される結果**:
```
❌ コード生成に失敗しました
エラー: Invalid authentication credentials
```

### 3.2 空のプロンプト

1. テキストモードでプロンプトを空にする
2. **作成開始** ボタンがdisabledになることを確認

### 3.3 画像なしで画像モード

1. 画像モードで画像をアップロードせずに作成
2. エラーメッセージが表示されることを確認:
   ```
   画像を選択してください
   ```

### 3.4 無効なURL

1. URLモードで無効なURLを入力: `not-a-url`
2. エラーメッセージが表示されることを確認

## テスト4: パフォーマンステスト

### 4.1 コード生成速度

- テキスト生成: 5-15秒以内
- 画像生成: 10-30秒以内（Vision APIは遅い）
- URLクローン: 10-20秒以内

### 4.2 レスポンスサイズ

```bash
# Workers APIのレスポンスサイズを確認
curl -X POST https://readdy-ai-workers.sayasaya.workers.dev/api/generate \
  -H "Content-Type: application/json" \
  -d '{"prompt":"Simple button","model":"gpt-4o","temperature":0.7,"max_tokens":500}' \
  -w "\nSize: %{size_download} bytes\nTime: %{time_total}s\n"
```

**期待される値**:
- レスポンスサイズ: 500-5000 bytes
- レスポンス時間: 5-15秒

## テスト5: セキュリティテスト

### 5.1 APIキー保護

1. ブラウザの開発者ツール > Network を開く
2. プロジェクト作成を実行
3. リクエストヘッダーにAPIキーが含まれていないことを確認
4. リクエストボディにAPIキーが含まれていないことを確認

**確認**: APIキーはWorkers側の環境変数で管理され、フロントエンドからは送信されない

### 5.2 CORS設定

```bash
# OPTIONSリクエストを送信
curl -X OPTIONS https://readdy-ai-workers.sayasaya.workers.dev/api/generate \
  -H "Origin: http://localhost:3000" \
  -v
```

**期待される結果**:
```
< Access-Control-Allow-Origin: *
< Access-Control-Allow-Methods: GET, POST, OPTIONS
```

### 5.3 XSS対策

1. プロンプトに以下を入力:
   ```
   <script>alert('XSS')</script>
   ```
2. 生成されたコードがエスケープされていることを確認

## テスト6: ブラウザ互換性

以下のブラウザで動作確認:

- [ ] Chrome/Edge (Chromium) - 最新版
- [ ] Firefox - 最新版
- [ ] Safari - 最新版（Mac/iOS）

## テスト結果レポート

### 成功基準

- [ ] すべてのWorkers APIエンドポイントが正常動作
- [ ] 3つの生成モード（テキスト/画像/URL）が動作
- [ ] 3つのデプロイ先（Vercel/GitHub Pages/Cloudflare Pages）が動作
- [ ] エラーハンドリングが適切
- [ ] APIキーが安全に管理されている
- [ ] レスポンス時間が許容範囲内

### 既知の問題

| 問題 | 優先度 | ステータス |
|------|--------|-----------|
| （記録） | （高/中/低） | （未対応/対応中/完了） |

## トラブルシューティング

### Workers APIが応答しない

```bash
# ログを確認
wrangler tail readdy-ai-workers

# 再デプロイ
cd workers/api
wrangler deploy
```

### フロントエンドでエラー

```bash
# 開発サーバーのログを確認
npm run dev

# ビルドエラーを確認
npm run build
```

### APIキーエラー

1. Workers APIの環境変数を再確認（`ENV_SETUP_GUIDE.md` 参照）
2. OpenAI APIの残高を確認
3. Cloudflare Dashboardで変数が暗号化されているか確認

## 次のステップ

テストが完了したら:

1. ✅ 本番環境へのデプロイ
2. ✅ ドキュメントの完成
3. ✅ ユーザーフィードバックの収集
4. ✅ パフォーマンス最適化

## サポート

テストに関する詳細情報:

- [Cloudflare Workers Testing](https://developers.cloudflare.com/workers/testing/)
- [Next.js Testing](https://nextjs.org/docs/testing)
- [OpenAI API Documentation](https://platform.openai.com/docs)
